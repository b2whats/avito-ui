{
  "name": "@avito/icons",
  "version": "0.0.9",
  "private": false,
  "description": "icons",
  "main": "register/index.js",
  "types": "register/index.d.ts",
  "files": ["register"],
  "scripts": {
    "build:types": "tsc -p tsconfig.json --emitDeclarationOnly",
    "build:js": "cross-env NODE_ENV=production babel src --out-dir register --extensions \".ts,.tsx\" --source-maps --root-mode=upward",
    "prebuild": "rm -rf ./register",
    "build": "yarn build:js && yarn build:types",
    "test": "echo \"Error: no test specified\" && exit 1",
    "typecheck": "tsc --noEmit"
  },
  "license": "MIT",
  "peerDependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "dependencies": {
    "@emotion/core": "^10.0.15",
    "@emotion/styled": "^10.0.15"
  },
  "devDependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "publishConfig": {
    "access": "restricted"
  },
  "gitHead": "11b9263aff30f1789f0a59d1bb5a003130ff524c"
}
