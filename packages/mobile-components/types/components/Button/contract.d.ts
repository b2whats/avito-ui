import '@avito/core';
declare module '@avito/core' {
    enum ButtonPreset {
        accent = 200,
        secondary,
        defaultOnSurface,
        defaultDark,
        outline,
        pay,
        appInstall,
        linkIncreased
    }
}
