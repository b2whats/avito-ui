type Variants = {
  primary: string,
  secondary: string,
  error: string,
  success: string,
  danger: string,
}

export const variants: Variants = {
  primary: 'blue',
  secondary: 'gray',
  error: 'red',
  success: 'green',
  danger: 'yellow',
}