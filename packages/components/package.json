{
  "name": "@avito/components",
  "version": "0.0.1",
  "private": false,
  "description": "components",
  "main": "build/index.tsx",
  "types": "build/index.d.ts",
  "files": [
    "build"
  ],
  "scripts": {
    "build": "yarn build:js && yarn build:types",
    "build:types": "tsc -p tsconfig.json --emitDeclarationOnly",
    "build:js": "cross-env NODE_ENV=production babel src --out-dir build --extensions \".ts,.tsx\" --delete-dir-on-start --source-maps --root-mode=upward",
    "test": "echo \"Error: no test specified\" && exit 1",
    "prepublishOnly": "yarn build"
  },
  "license": "MIT",
  "peerDependencies": {
    "react": "^16.8.6",
    "react-dom": "^16.8.6"
  },
  "dependencies": {
    "@avito/tokens": "^0.0.1",
    "@emotion/core": "^10.0.10",
    "@emotion/styled": "^10.0.12",
    "emotion-theming": "^10.0.10"
  },
  "publishConfig": {
    "access": "public"
  }
}
