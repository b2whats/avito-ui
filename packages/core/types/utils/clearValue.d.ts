/**
 * clear editable element using execCommand to preserve undo history
 * NOTE: element must be focusable — not disabled / readOnly
 */
export declare function clearValue(element: HTMLElement | null): void;
